# Modelos

Os pesos `.pt` não são versionados no GitHub.

Para treinar ou inferir com o pipeline original, coloque aqui o peso base:

- `yolo26n-seg.pt`

O peso treinado final (`best.pt`) também foi removido do repositório por ser artefato pesado. Ele deve ser regenerado com o script de treinamento ou armazenado em uma release, drive externo ou Git LFS.

